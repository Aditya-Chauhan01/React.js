import React from 'react'

const UserContext = React.createContext()      // this creats a Context object that can be used to provide and consume values in the component tree. context mean it is like global variable where all datas are stored. 

export default UserContext;